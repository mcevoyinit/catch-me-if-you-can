version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ENVIRONMENT=development
      - DEBUG=True
      - API_HOST=0.0.0.0
      - API_PORT=5000
    volumes:
      - .:/app
    command: python main.py api --host 0.0.0.0 --port 5000